import _extends from "@babel/runtime/helpers/esm/extends";
import { createElement } from "@wordpress/element";

/**
 * WordPress dependencies
 */
import { FontSizePicker as BaseFontSizePicker } from '@wordpress/components';
import { useSelect } from '@wordpress/data';
/**
 * Internal dependencies
 */

import useEditorFeature from '../use-editor-feature';

function FontSizePicker(props) {
  var fontSizes = useSelect(function (select) {
    return select('core/block-editor').getSettings().fontSizes;
  }, []);
  var disableCustomFontSizes = !useEditorFeature('fontSize.custom');
  return createElement(BaseFontSizePicker, _extends({}, props, {
    fontSizes: fontSizes,
    disableCustomFontSizes: disableCustomFontSizes
  }));
}

export default FontSizePicker;
//# sourceMappingURL=font-size-picker.js.map