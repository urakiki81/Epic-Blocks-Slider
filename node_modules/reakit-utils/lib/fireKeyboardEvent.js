'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

require('./getDocument.js');
require('./getDefaultView.js');
var createKeyboardEvent = require('./createKeyboardEvent.js');

/**
 * Creates and dispatches `KeyboardEvent` in a way that also works on IE 11.
 *
 * @example
 * import { fireKeyboardEvent } from "reakit-utils";
 *
 * fireKeyboardEvent(document.getElementById("id"), "keydown", {
 *   key: "ArrowDown",
 *   shiftKey: true,
 * });
 */

function fireKeyboardEvent(element, type, eventInit) {
  return element.dispatchEvent(createKeyboardEvent.createKeyboardEvent(element, type, eventInit));
}

exports.fireKeyboardEvent = fireKeyboardEvent;
