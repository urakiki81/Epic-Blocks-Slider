'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

/**
 * Checks whether `arg` is a promise or not.
 *
 * @returns {boolean}
 */
function isPromise(arg) {
  return Boolean(arg && arg.then);
}

exports.isPromise = isPromise;
